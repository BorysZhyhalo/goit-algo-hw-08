"""
Завдання 3
Уявіть, що вам на технічному інтерв'ю дають наступну задачу, яку треба розв'язати за допомогою купи.

Є декілька мережевих кабелів різної довжини, їх потрібно об'єднати по два за раз в один кабель, 
використовуючи з'єднувачі, у порядку, який призведе до найменших витрат. 
Витрати на з'єднання двох кабелів дорівнюють їхній сумі довжин, а загальні витрати дорівнюють 
сумі з'єднання всіх кабелів.

Завдання полягає в тому, щоб знайти порядок об'єднання, який мінімізує загальні витрати.
"""

import heapq

def min_connect_cost(cables):
    """
    Мінімальні загальні витрати на з'єднання всіх кабелів.
    cables: список довжин кабелів.
    """
    if len(cables) <= 1:
        return 0

    heapq.heapify(cables)  # перетворити список на min-heap
    heap = cables[:]  # створити копію для роботи з heap
    total_cost = 0

    while len(heap) > 1:
        min_1 = heapq.heappop(heap)  # найкоротший
        min_2 = heapq.heappop(heap)  # другий найкоротший

        combined = min_1 + min_2
        total_cost += combined

        heapq.heappush(heap, combined)  # новий кабель назад у купу

    return total_cost


if __name__ == "__main__":
    cables = [4, 3, 2, 6]
    print(min_connect_cost(cables))
    
    
"""
Пояснення:
    
Задача жадібна: на кожному кроці беремо два мінімальні елементи.
Структура даних: heapq (min-heap).
Сума витрат = сума довжин усіх проміжних з’єднань.
3+2=5 (витрати 5), новий кабель 5
4+5=9 (витрати 9), новий кабель 9
6+9=15 (витрати 15), новий кабель 15
Загальні витрати = 5 + 9 + 15 = 29.
"""